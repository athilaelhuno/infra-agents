# infra-agents Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-16

## Active Technologies

- Python 3.11+ + `pydantic` (for structured tool inputs), `typer` (for the orchestrator CLI), `subprocess` (native, for CLI tool execution), `requests` (for lightweight API calls where CLI isn't viable). (001-infra-automation-agents)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11+: Follow standard conventions

## Recent Changes

- 001-infra-automation-agents: Added Python 3.11+ + `pydantic` (for structured tool inputs), `typer` (for the orchestrator CLI), `subprocess` (native, for CLI tool execution), `requests` (for lightweight API calls where CLI isn't viable).

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
